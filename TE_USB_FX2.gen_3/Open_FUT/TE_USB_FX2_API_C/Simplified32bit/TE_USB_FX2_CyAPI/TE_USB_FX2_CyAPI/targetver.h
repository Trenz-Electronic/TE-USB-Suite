#pragma once

// Add-in SDKDDKVer.h to define the Windows base mostly available.

// If it is desired to compile the application for this once Windows base preceding, add-in WinSDKVer.h and 
// set up the macro _WIN32_WINNT on the base to support before add-in SDKDDKVer.h

#include <SDKDDKVer.h>
